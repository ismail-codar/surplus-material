import * as Surplus from 'surplus';
Surplus;
import { S } from "surplus";
import { DataSignal } from "s-js";
import { MdcPersistentDrawer } from "../../components/drawer";

export = (ctrl: any) => {
    return <section>
        <MdcPersistentDrawer header={null} open={null}>

        </MdcPersistentDrawer>
    </section>
}