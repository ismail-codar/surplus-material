import * as Surplus from 'surplus';
Surplus;
import { S } from "surplus";
import { DataSignal } from "s-js";
import { mdcRippleSurfaceElement } from "../components/_css-dom-utils";

export = (ctrl: any) => {
    return mdcRippleSurfaceElement(
        <section className="hero">
        </section>
    )
}